// import { UseData } from "../context/dataContext";
import DateRange from "./dateRange";

const Header = () => {
  // const { data } = UseData();

  
  return (
    <>
        <DateRange />
    </>
  )
};

export default Header;
